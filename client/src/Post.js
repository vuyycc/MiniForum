/*
    <div>
                <img id="author-img" src={'http://localhost:8797/' + item.author.avatar}></img>
                    <label for="author-img">
                        {item.author.name}
                    </label>
                    
                </div>
                <h2>[{item.title}]</h2>
                <div> {item.described}</div>
                <div>
                    {item?.imgVideo}
                </div>
*/


import React from 'react'

export default function Post() {
    //const getUserReducer = useSelector(state => state.getUserReducer)
    return (
        <div>
            Halo
        </div>
    )
}
